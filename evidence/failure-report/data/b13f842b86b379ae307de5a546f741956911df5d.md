# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: e2e\price-regression.spec.ts >> DEMO-01 detects an intentionally injected one-cent price regression
- Location: evidence-demo\e2e\price-regression.spec.ts:3:1

# Error details

```
Error: expect(locator).toHaveText(expected) failed

Locator:  getByTestId('total')
Expected: "€22.95"
Received: "€22.96"
Timeout:  5000ms

Call log:
  - Expect "toHaveText" getByTestId('total') with timeout 5000ms
  - waiting for getByTestId('total')
    14 × locator resolved to <strong data-testid="total">€22.96</strong>
       - unexpected value "€22.96"

```

```yaml
- strong: €22.96
```

# Test source

```ts
  1  | import { test, expect } from '../../tests/fixtures.js';
  2  | 
  3  | test('DEMO-01 detects an intentionally injected one-cent price regression', async ({ page, shop }) => {
  4  |   await page.route('**/api/products', async route => {
  5  |     const response = await route.fetch();
  6  |     const body = await response.json();
  7  |     body.products.find((product: { id: string }) => product.id === 'bottle').priceCents = 1801;
  8  |     await route.fulfill({ response, json: body });
  9  |   });
  10 |   await shop.open(); await shop.add('Trail Bottle'); await shop.viewCart();
  11 |   // Deliberately fails: real UI displays €22.96 after the injected price change.
> 12 |   await expect(page.getByTestId('total')).toHaveText('€22.95');
     |                                           ^ Error: expect(locator).toHaveText(expected) failed
  13 | });
  14 | 
```